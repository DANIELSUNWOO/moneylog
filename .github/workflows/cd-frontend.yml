name: CD - Frontend

on:
  push:
    branches: [main]
    paths:
      - "frontend/**"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: 체크아웃
        uses: actions/checkout@v4

      - name: GHCR 로그인
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 빌드 + 푸시
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          push: true
          tags: |
            ghcr.io/danielsunwoo/moneylog-frontend:latest
            ghcr.io/danielsunwoo/moneylog-frontend:${{ github.sha }}

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - name: AWS 자격 증명 설정 (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ap-northeast-2

      - name: EC2에서 재배포 (SSM)
        run: |
          COMMAND_ID=$(aws ssm send-command \
            --instance-ids "i-0ee7f6a4eade2fdbb" \
            --document-name "AWS-RunShellScript" \
            --comment "CD - Frontend deploy" \
            --parameters '{"commands":["cd /home/ec2-user/moneylog","docker compose -f docker-compose.yml pull frontend","docker compose -f docker-compose.yml up -d frontend"]}' \
            --query "Command.CommandId" --output text)
          echo "Command ID: $COMMAND_ID"

          aws ssm wait command-executed \
            --command-id "$COMMAND_ID" \
            --instance-id "i-0ee7f6a4eade2fdbb" || true

          aws ssm get-command-invocation \
            --command-id "$COMMAND_ID" \
            --instance-id "i-0ee7f6a4eade2fdbb"

          STATUS=$(aws ssm get-command-invocation \
            --command-id "$COMMAND_ID" \
            --instance-id "i-0ee7f6a4eade2fdbb" \
            --query "Status" --output text)

          if [ "$STATUS" != "Success" ]; then
            echo "배포 실패: 위 StandardErrorContent를 확인하세요."
            exit 1
          fi